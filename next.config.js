/** @type {import('next').NextConfig} */
const nextConfig = {}

nextConfig.images = {
    domains: [
        'utfs.io',
        'avatar.iran.liara.run',
        'snapsite-domains.s3.ap-south-1.amazonaws.com',
    ]
}

module.exports = nextConfig
